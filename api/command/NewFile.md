# NewFile

```lua
app.command.NewFile {
  ui=true,
  width=0,
  height=0,
  colorMode=ColorMode.RGB,
  fromClipboard=false
}
```

* `ui`
* `width`
* `height`
* `colorMode` ([ColorMode](../colormode.md#colormode))
* `fromClipboard`
